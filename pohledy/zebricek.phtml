<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MapujTo | Přihlášení</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="css/stylZebr.css">
</head>
<body>

<div class="container py-5">
    <h2 class="text-center mb-4">🏆 Žebříček turistů</h2>
    <div class="row justify-content-center">
        <?php foreach ($users as $index => $user): ?>
            <div class="col-md-4 d-flex justify-content-center mb-4">
                <div class="card shadow rounded-4 text-center p-3" style="min-width: 260px;">
                    <div class="card-body">
                        <h5 class="card-title">#<?= $index + 1 ?> <?= htmlspecialchars($user['Username']) ?></h5>
                        <p class="card-text">🥇 Body: <?= (int)$user['Points'] ?></p>
                        <p class="card-text">📶 Rank: <?= htmlspecialchars($user['Rank']) ?></p>
                    </div>
                </div>
            </div>
        <?php endforeach; ?>
    </div>
</div>

</body>