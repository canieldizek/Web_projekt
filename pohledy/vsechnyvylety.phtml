<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MapujTo | Přihlášení</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="css/styl.css">
</head>
<body>
    <h2>Všechny túry</h2>

<?php if (!isset($_SESSION['User']['ID_user'])): ?>
    <p>Pro zobrazení svých výletů se musíte přihlásit.</p>
<?php elseif (!empty($tripData)): ?>
    <table cellpadding="8" cellspacing="0">
        <thead>
            <tr>
                <th>Název túry</th>
                <th>Délka (km)</th>
                <th>Čas</th>
                <th>Obtížnost</th>
                <th>Uživatel</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($tripData as $trip): ?>
                <tr>
                    <td><?= htmlspecialchars($trip['Name_trip']) ?></td>
                    <td><?= $trip['Length'] ?></td>
                    <td><?= $trip['Time'] ?></td>
                    <td><?= htmlspecialchars($trip['Difficulty']) ?></td>
                    <td><?= htmlspecialchars($trip['Creator']) ?></td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
<?php else: ?>
    <p>Nemáte zatím žádné výlety.</p>
<?php endif; ?>


</body>